const grid=document.getElementById('projectGrid');function renderProjects(){const s=getState();grid.innerHTML=PROJECTS.map(p=>{const done=s.completedProjects.includes(p.id);return `<article class="project-card ${done?'done':''}"><div class="project-meta"><span class="pill">${p.emoji} Project</span><span class="pill">+${p.xp} XP</span></div><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p><div class="project-meta">${p.skills.map(x=>`<span class="pill">${esc(x)}</span>`).join('')}</div><div class="task-actions"><button class="btn ${done?'btn-secondary':'btn-primary'}" data-id="${p.id}">${done?'✓ Completed':'Mark Project Complete'}</button></div></article>`}).join('');grid.querySelectorAll('button[data-id]').forEach(b=>b.onclick=()=>{toggleProject(b.dataset.id);renderProjects()})}renderProjects();
