const s=getState(),xp=currentXP(s),unlocked=BADGES.filter(b=>b.check(s));document.getElementById('levelBanner').innerHTML=`<span class="eyebrow">CURRENT LEVEL</span><h2>${getLevel(xp)}</h2><p class="muted">You have ${xp} XP and ${unlocked.length}/${BADGES.length} achievements unlocked.</p><div class="module-progress"><span style="width:${Math.min(100,(xp/6000)*100)}%"></span></div>`;document.getElementById('badgeGrid').innerHTML=BADGES.map(b=>{const yes=b.check(s);return `<article class="badge-card ${yes?'':'locked'}"><span class="badge-status">${yes?'UNLOCKED':'LOCKED'}</span><div class="badge-icon">${b.icon}</div><h3>${esc(b.title)}</h3><p>${esc(b.description)}</p></article>`}).join('');
