const s=getState(),xp=currentXP(s),quizCount=Object.keys(s.quizScores).length;document.getElementById('progressSummary').innerHTML=[['Chapters',`${s.completedChapters.length}/53`],['Tasks',`${s.completedTasks.length}/${TASKS.length}`],['Quizzes',`${quizCount}/53`],['XP',xp]].map(x=>`<article class="progress-card"><h3>${x[0]}</h3><strong>${x[1]}</strong></article>`).join('');document.getElementById('moduleProgressList').innerHTML=COURSE_MODULES.map(m=>{const cs=CHAPTERS.filter(c=>c.module===m.id),done=cs.filter(c=>s.completedChapters.includes(c.id)).length;return `<article class="module-progress-row"><div class="row-top"><span>${m.icon} ${esc(m.title)}</span><span>${done}/${cs.length}</span></div><p>${esc(m.description)}</p><div class="module-progress"><span style="width:${percent(done,cs.length)}%"></span></div></article>`}).join('');
