document.getElementById('themeToggle').onclick=()=>{const s=getState();s.theme=s.theme==='light'?'dark':'light';saveState(s);toast(`Theme changed to ${s.theme}.`)};document.getElementById('resetData').onclick=()=>{if(confirm('Reset all saved JavaScript Mastery progress on this browser?')){localStorage.removeItem(STORAGE_KEY);applyTheme();renderChrome();toast('Course data has been reset.')}};
