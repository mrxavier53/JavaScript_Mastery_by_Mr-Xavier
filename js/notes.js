const select=document.getElementById('notesChapter'),editor=document.getElementById('notesEditor'),msg=document.getElementById('saveMessage');select.innerHTML=CHAPTERS.map(c=>`<option value="${c.id}">Chapter ${c.id}: ${esc(c.title)}</option>`).join('');function load(){editor.value=getState().notes[select.value]||'';msg.textContent=''}select.onchange=load;document.getElementById('saveNotes').onclick=()=>{const s=getState();s.notes[select.value]=editor.value;saveState(s);msg.textContent='Saved on this browser ✓';setTimeout(()=>msg.textContent='',2500)};load();
